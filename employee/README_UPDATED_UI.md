# Employee Portal UI Update

Updated the employee portal shared layout to match the Super Admin visual language from the provided reference:

- Fixed dark navy left sidebar
- Fixed white top navigation bar
- Purple active navigation state and buttons
- ArdhnariShwar branding using the existing logo
- Employee name/department in sidebar and top bar
- Theme switcher (Light/Dark)
- Notifications icon
- Responsive mobile hamburger drawer
- Existing employee page content/routes preserved
- Existing API/backend code was not changed

The `dist/` folder is also updated for direct static hosting. The source changes are in `src/components/layout/EmployeeNavbar.jsx` and `src/index.css`.
