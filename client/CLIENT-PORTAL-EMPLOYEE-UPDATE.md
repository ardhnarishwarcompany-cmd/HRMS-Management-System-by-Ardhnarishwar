# Client Portal / Employee Login Update

Frontend updated for role-aware Client Admin vs Client Employee experiences.

Employee portal now shows only the permitted modules and own records. Client administrators retain the management modules.

The client source is supplied with the updated Vite/React source. Run `npm install` and `npm run build` after replacing the existing client source if you deploy the generated `dist` folder.
